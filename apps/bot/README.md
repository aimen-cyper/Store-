# Telegram Admin Bot

The bot is intentionally a thin interface over the API. Configure `TELEGRAM_BOT_TOKEN`; authorization is enforced by the backend. Do not duplicate order, wallet, or catalog business logic here.
