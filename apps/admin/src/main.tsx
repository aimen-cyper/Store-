import React,{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import'./style.css';const API=import.meta.env.VITE_API_URL||'http://localhost:8000';function App(){const[t,setT]=useState(localStorage.getItem('admin_token')||'');const[m,setM]=useState<any>();const[s,setS]=useState<any[]>([]);async function me(){const r=await fetch(API+'/api/v1/auth/me',{headers:{Authorization:`Bearer ${t}`}});if(r.ok)setM(await r.json())}async function services(){const r=await fetch(API+'/api/v1/catalog/services');setS(await r.json())}useEffect(()=>{if(t)me();services()},[t]);return <main><h1>DHĀT ADMIN</h1>{m?<><p>المستخدم: {m.username||m.telegram_id} — الدور: {m.role}</p><h2>الخدمات</h2>{s.map(x=><div className="row" key={x.id}>{x.name}<b>{x.price} {x.currency}</b></div>)}</>:<p>سجّل الدخول عبر Telegram ثم ضع session token هنا.</p>}<p className="note">كل العمليات الإدارية تمر عبر API وتخضع للصلاحيات.</p></main>}createRoot(document.getElementById('root')!).render(<App/>);
